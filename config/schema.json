{"format_version":"1.0","provider_schemas":{"registry.terraform.io/hops-ops/listmonk":{"provider":{"version":0,"block":{"attributes":{"endpoint":{"type":"string","description":"Base URL of the Listmonk API (e.g. `http://marketing-listmonk.marketing.svc.cluster.local:9000`). The provider appends `/api/...` to each request. Falls back to the `LISTMONK_ENDPOINT` environment variable.","description_kind":"markdown","optional":true},"token":{"type":"string","description":"Password / token of the Listmonk API user. For `api`-typed users this is the value returned at User creation. Falls back to `LISTMONK_TOKEN`.","description_kind":"markdown","optional":true,"sensitive":true},"username":{"type":"string","description":"Username of the Listmonk API user (an `api`-typed User, ideally `crossplane-provider`). Falls back to `LISTMONK_USERNAME`.","description_kind":"markdown","optional":true}},"description":"Terraform provider for Listmonk (https://listmonk.app). Manages per-section settings rows (e.g. `security.oidc`), user roles, API users, lists, list roles, and templates via the Listmonk REST API.","description_kind":"markdown"}},"resource_schemas":{"listmonk_app_settings":{"version":0,"block":{"attributes":{"concurrency":{"type":"number","description":"Number of parallel worker goroutines used for campaign sending. Maps to `app.concurrency`.","description_kind":"markdown","optional":true},"favicon_url":{"type":"string","description":"Public URL to a favicon used in the admin UI + subscriber pages. Maps to `app.favicon_url`.","description_kind":"markdown","optional":true},"from_email":{"type":"string","description":"Default From: address used when sending campaign mail. Maps to `app.from_email`.","description_kind":"markdown","optional":true},"id":{"type":"string","description":"Synthetic ID for the singleton app settings group. Always `app`.","description_kind":"markdown","computed":true},"lang":{"type":"string","description":"Default UI language code (e.g. `en`, `de`, `fr`). Maps to `app.lang`.","description_kind":"markdown","optional":true},"logo_url":{"type":"string","description":"Public URL to a logo image used in the admin UI + subscriber pages. Maps to `app.logo_url`.","description_kind":"markdown","optional":true},"message_rate":{"type":"number","description":"Max messages per second per concurrency worker. Maps to `app.message_rate`.","description_kind":"markdown","optional":true},"root_url":{"type":"string","description":"Public root URL Listmonk uses when constructing absolute links (subscription confirm, click-tracking, OIDC redirect URI). E.g. `https://marketing.example.com`. Maps to `app.root_url`.","description_kind":"markdown","optional":true},"site_name":{"type":"string","description":"Brand name shown in the admin UI + subscriber-facing pages. Maps to `app.site_name`.","description_kind":"markdown","optional":true}},"description":"Listmonk app/general settings — the `app.*` rows of the settings table. Singleton per Listmonk instance. Each field is independently managed: a field declared in HCL becomes the source of truth; a field left null is not touched on the server, so operator UI edits to unmanaged fields are preserved.\n\nv0.1 covers the most common knobs (root URL, from-email, site name, branding, lang, concurrency, message rate). Less-common fields (`app.batch_size`, `app.max_send_errors`, etc.) can be added in later versions of the provider.","description_kind":"markdown"}},"listmonk_security_settings":{"version":0,"block":{"attributes":{"id":{"type":"string","description":"Synthetic ID for the singleton settings section. Always `security`.","description_kind":"markdown","computed":true}},"block_types":{"oidc":{"nesting_mode":"single","block":{"attributes":{"auto_create_users":{"type":"bool","description":"When true, sign-in by an unknown OIDC subject creates a Listmonk user assigned the role identified by `default_user_role_id`. Listmonk's OIDC subject-to-role mapping does not consume `groups`/`roles` claims — see the provider README for the explicit-allowlist alternative.","description_kind":"markdown","optional":true},"client_id":{"type":"string","description":"OIDC application client ID.","description_kind":"markdown","optional":true},"client_secret":{"type":"string","description":"OIDC application client secret. Write-only — Listmonk masks the value on read.","description_kind":"markdown","optional":true,"sensitive":true},"default_list_role_id":{"type":"number","description":"List role ID assigned to auto-created users. Leave null to leave Listmonk's default in place.","description_kind":"markdown","optional":true},"default_user_role_id":{"type":"number","description":"User role ID assigned to auto-created users (typically a `listmonk_user_role` ID). Leave null to leave Listmonk's default in place.","description_kind":"markdown","optional":true},"enabled":{"type":"bool","description":"Enable OIDC sign-in.","description_kind":"markdown","required":true},"provider_name":{"type":"string","description":"Display name shown on the sign-in page (e.g. `Zitadel`).","description_kind":"markdown","optional":true},"provider_url":{"type":"string","description":"OIDC issuer base URL (e.g. `https://auth.example.com`). Listmonk fetches `\u003cprovider_url\u003e/.well-known/openid-configuration` to discover endpoints.","description_kind":"markdown","optional":true}},"description":"OpenID Connect SSO configuration (the `security.oidc` row). When this block is omitted the provider does not touch the server-side OIDC settings.","description_kind":"markdown"}}},"description":"Listmonk Security settings — the `security.*` rows of the `settings` table. Singleton resource per Listmonk instance (one `security` section per server). v0.1 manages the `security.oidc` sub-block; future versions will add `security.captcha` and `security.basic_auth`.\n\n**Drift behavior**: only sub-blocks declared in this resource are reconciled. Operator UI edits to undeclared sub-blocks (e.g. CAPTCHA) are preserved. Operator UI edits to *managed* sub-blocks are reverted on the next `terraform apply`.\n\n**Client secret**: Listmonk masks `oidc.client_secret` with bullet characters on `GET /api/settings`, so the provider keeps the value Terraform last wrote authoritative in state — drift on `client_secret` cannot be detected and will only re-converge on the next apply. Rotate by changing the HCL value and applying.","description_kind":"markdown"}},"listmonk_user":{"version":0,"block":{"attributes":{"created_at":{"type":"string","description":"RFC 3339 timestamp.","description_kind":"markdown","computed":true},"email":{"type":"string","description":"Unique email address. For `api` users this doesn't need to be deliverable but must still be unique.","description_kind":"markdown","required":true},"id":{"type":"string","description":"Numeric user ID assigned by Listmonk (stringified for TF state).","description_kind":"markdown","computed":true},"list_role_id":{"type":"number","description":"Optional ID of a list role (`listmonk_list_role`) that defines this user's per-list access. Null leaves it unset.","description_kind":"markdown","optional":true},"name":{"type":"string","description":"Display name shown in the Listmonk admin UI.","description_kind":"markdown","required":true},"password":{"type":"string","description":"For `type = api`: the plaintext token used for HTTP Basic-Auth. For `type = user`: the operator-chosen password (Listmonk stores it bcrypt-hashed; never returned on Read so drift on it is undetectable — rotate by changing the HCL value and applying).","description_kind":"markdown","optional":true,"sensitive":true},"password_login":{"type":"bool","description":"Whether this user can sign in via the Listmonk admin UI form. For `api` users set false. For `user` users typically true.","description_kind":"markdown","required":true},"status":{"type":"string","description":"Lifecycle status: `enabled` or `disabled`. Disabled users can't authenticate but their row is preserved.","description_kind":"markdown","required":true},"type":{"type":"string","description":"User type: `user` (interactive) or `api` (machine credential whose `password` is the plaintext Basic-Auth token).","description_kind":"markdown","required":true},"updated_at":{"type":"string","description":"RFC 3339 timestamp.","description_kind":"markdown","computed":true},"user_role_id":{"type":"number","description":"ID of the user role (`listmonk_user_role`) that defines this user's permissions. Use `1` for the Listmonk-seeded `Super Admin` role.","description_kind":"markdown","required":true},"username":{"type":"string","description":"Unique username. Becomes the principal name for Basic-Auth on `api` users; the display name in the Listmonk admin UI for `user` users.","description_kind":"markdown","required":true}},"description":"A Listmonk User. Two types: `user` (interactive admin) and `api` (machine credential). For `api` users the password IS the plaintext token used for HTTP Basic-Auth — Listmonk doesn't have a separate token-mint endpoint. After creating a new `api` user, the Listmonk pod must be restarted before the credential is recognized (the apiUsers cache is loaded only at startup).","description_kind":"markdown"}},"listmonk_user_role":{"version":0,"block":{"attributes":{"created_at":{"type":"string","description":"RFC 3339 timestamp.","description_kind":"markdown","computed":true},"id":{"type":"string","description":"Numeric role ID assigned by Listmonk (stringified for TF state). Stable for the lifetime of the role.","description_kind":"markdown","computed":true},"name":{"type":"string","description":"Human-readable role name. Shown in the Listmonk admin UI's role picker.","description_kind":"markdown","required":true},"permissions":{"type":["set","string"],"description":"Set of dotted-string permissions granted by this role. See Listmonk's source for the canonical list (e.g. `settings:get`, `settings:manage`, `users:get`, `users:manage`, `roles:get`, `roles:manage`, `lists:get_all`, `lists:manage_all`, `templates:get`, `templates:manage`, `campaigns:get`, `campaigns:manage`, `subscribers:manage`, `bounces:get`, `bounces:manage`, `media:get`, `media:manage`, `tx:send`).","description_kind":"markdown","required":true},"updated_at":{"type":"string","description":"RFC 3339 timestamp.","description_kind":"markdown","computed":true}},"description":"A Listmonk User Role — a named bundle of dotted-string permissions (`settings:manage`, `users:get`, `lists:manage_all`, etc.). Assigned to Users via the `user_role_id` field. The `Super Admin` role (id=1) is seeded by Listmonk's install migration and should not be managed by Terraform.","description_kind":"markdown"}}}}}}
